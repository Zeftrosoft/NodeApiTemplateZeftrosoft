aside.main-sidebar.sidebar-dark-primary.elevation-4
  // Brand Logo
  a.brand-link(href='/')
    img.brand-image.img-circle.elevation-3(src='/dist/img/AdminLTELogo.png', alt='AdminLTE Logo', style='opacity: .8')
    span.brand-text.font-weight-light Display Aware
  // Sidebar
  .sidebar
    // Sidebar Menu
    nav.mt-2
      ul.nav.nav-pills.nav-sidebar.flex-column(data-widget='treeview', role='menu', data-accordion='false')
        //
          Add icons to the links using the .nav-icon class
          with font-awesome or any other icon font library
        
        li.nav-item
          a.nav-link(href='/logout')
            i.nav-icon.fas.fa-lock
            p
              | Logout

        if role == 'super'      
          li.nav-item
            a.nav-link(href='/device')
              i.nav-icon.fas.fa-tag
              p
                | Device 
        li.nav-item
          a.nav-link(href='/')
            i.nav-icon.fas.fa-th
            p
              | Dashboard

    // /.sidebar-menu
    // /.sidebar
      